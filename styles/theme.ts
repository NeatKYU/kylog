import { toRem } from "@/lib/helper";

const colors = {
	black: '#242424',
	white: '#FFF',

	primaryColor: '#969BCE',
	secondColor: '#aaadcc',
	hoverColor: '#999ed6',
}

const theme = {
	colors,
}

export default theme;